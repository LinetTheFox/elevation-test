name: elevation_test
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter  
  cupertino_icons: ^1.0.2
  dio: ^4.0.4
  geolocator: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  

flutter:
  uses-material-design: true
